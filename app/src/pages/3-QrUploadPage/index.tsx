import React from 'react'
import QrUploadMobile from './QrUploadMobile'

/**
 * QrUploadPage - QR-based image upload page
 *
 * Features:
 * - Mobile image upload via QR code token
 * - File selection and upload to server
 * - Progress tracking and error handling
 * - Automatic try-on process initiation
 */
export default function QrUploadPage() {
  return <QrUploadMobile />
}
